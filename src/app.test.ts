import { describe, test } from "bun:test";

describe("App", () => {
  test("simple", () => {
    // const app = App.new({
    //   libs: { stdio, stdstring },
    //   main({ libs: { stdio, stdstring } }) {
    //     return [stdio.puts.call(Lit.string("abc"))];
    //   },
    // });
    // expect(app.create()).toBe(
    //   `#include <stdio.h>\nint main(void)\n{\nputs("abc");\n}`
    // );
  });
});
